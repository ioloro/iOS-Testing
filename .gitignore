### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
*.pyc

### Swift Package Manager (cli/)
cli/.build/
cli/.swiftpm/
# Package.resolved IS committed — locks dependency versions for reproducible builds of the executable

### v0.2 framework vendoring — built by scripts/fetch-frameworks.sh
cli/Frameworks/

### iostesting per-project config (user state, not source)
.iostesting/
